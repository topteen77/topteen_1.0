fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.1
PyJWT>=2.9.0
pydantic>=2.9.0

# For `GET /api/counselor/{coun_id}/dashboard` (Django ORM + `topteens.settings`), also install the main app:
#   pip install -r ../requirements.txt
