# Deploy TopTeens via Docker (web-only for CI)
# Triggers on push to master/main. Uses deploy.sh web: build web image only, update web
# container, migrate, collectstatic. mysql, redis, celery, nginx stay running.
#
# Required secrets: AWS_SSH_KEY, AWS_HOST, AWS_USER (already set up)
# Optional: DEPLOY_PATH (default: /home/ubuntu/topteens_new/topteen_1.0)
#


# name: Deploy Docker (Web Only)

# on:
#   push:
#     branches:
#       - deploy-docker
# #      - master
# #      - main

# jobs:
#   deploy-web:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up SSH
#         run: |
#           mkdir -p ~/.ssh
#           echo "${{ secrets.AWS_SSH_KEY }}" > ~/.ssh/id_ed25519
#           chmod 600 ~/.ssh/id_ed25519
#           ssh-keyscan -H ${{ secrets.AWS_HOST }} >> ~/.ssh/known_hosts 2>/dev/null || true

#       - name: Deploy web via SSH (Docker)
#         env:
#           DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
#         run: |
#           ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no ${{ secrets.AWS_USER }}@${{ secrets.AWS_HOST }} bash -s << 'ENDSSH'
#             set -e
#             DEPLOY_PATH="${DEPLOY_PATH:-/home/ubuntu/topteens_new/topteen_1.0}"
#             cd "$DEPLOY_PATH" || { echo "Deploy path not found: $DEPLOY_PATH"; exit 1; }
#             echo "[CI] Pulling latest code..."
#             git pull origin master || git pull origin main || true
#             echo "[CI] Running deploy.sh web (build web only, update web container, migrate, collectstatic)..."
#             ./deploy.sh web
#             echo "[CI] Web deploy complete."
#           ENDSSH
